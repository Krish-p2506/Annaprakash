# Annaprakash
# First Model is train on only one plant later on we will add  it
# WE Train model to classifies diseases of plant using CNN
# model train on train_model.py---->(run)--->we get lemon_disease_model.h5
# and this .h5 file convert into tensorflowLite by conert_to_tflite.py file
# This TensorFlow file is called Edge AI Brain(offline)